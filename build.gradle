plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    compileOnly files('libs/jpipeline-common.jar')
}

jar {
    from {
        configurations.runtimeClasspath.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
}
